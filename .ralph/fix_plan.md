# Fix Plan — SmartFinances (Rich-Uncle-Scrooge)

## Фаза 0: Инициализация живой документации

- [ ] Создать файл `docs/ARCHITECTURE.md` с полным описанием ТЕКУЩЕГО состояния проекта (до начала изменений)
- [ ] Задокументировать: структуру файлов, все модули, все модели БД (User, Account, Transaction, PendingAction), все callback_data форматы, все интенты, переменные окружения
- [ ] Задокументировать основные flows: обработка текстового сообщения, обработка голосового, подтверждение операции, отчёты, аналитика, Google Sheets
- [ ] Задокументировать нюансы: retry monkeypatch в handlers.py, каскадный LLM (4o-mini → 4o), формат callback_data `fin:action:param1:param2`, дефолтный счёт auto-set
- [ ] Добавить раздел «Changelog» в конец файла
- [ ] **ПОСЛЕ КАЖДОЙ последующей задачи из этого плана — обновлять docs/ARCHITECTURE.md** (добавлять новые модули, модели, flows, callback-форматы, записывать в changelog)

## Фаза 1: Кнопочное меню и UX

### 1.1 Инфраструктура меню
- [ ] Создать `bot/menu.py` — модуль с функциями построения клавиатур (ReplyKeyboardMarkup, InlineKeyboardMarkup)
- [ ] Создать функцию `main_menu_keyboard()` — persistent reply-клавиатура: 💰 Счета, 📊 Отчёты, 📝 Записать, ⚙️ Настройки, ❓ Помощь
- [ ] Создать функцию `back_to_menu_button()` — inline-кнопка «◀️ Главное меню» для добавления в каждый ответ
- [ ] Добавить обработку нажатий reply-кнопок в handlers.py (через MessageHandler с фильтром по тексту кнопок)
- [ ] Написать тесты для `bot/menu.py` — корректность клавиатур и кнопок

### 1.2 Онбординг нового пользователя
- [ ] Переработать `start_command`: если нет счетов → пошаговый онбординг с кнопками
- [ ] Шаг 1: Приветствие + описание бота + кнопка «🚀 Начать»
- [ ] Шаг 2: Выбор валюты первого счёта кнопками (RUB, USD, EUR, USDT)
- [ ] Шаг 3: Ввод названия счёта (текстом или предложить варианты кнопками: Карта, Наличка, Крипто)
- [ ] Шаг 4: Опциональный начальный баланс (кнопка «Пропустить» или ввод суммы)
- [ ] Шаг 5: Счёт создан → показать главное меню
- [ ] Если счета уже есть — показать приветствие + главное меню (как сейчас, но с кнопками)
- [ ] Добавить ConversationHandler для пошагового диалога онбординга
- [ ] Написать тесты для онбординга

### 1.3 Подменю «Счета»
- [ ] По нажатию «💰 Счета» — показать список счетов с балансами + inline-кнопки: «➕ Создать счёт», «⭐ Назначить главный», «◀️ Главное меню»
- [ ] Создание счёта через пошаговый диалог кнопками (валюта → название → баланс) — аналогично онбордингу
- [ ] «Назначить главный» — показать список счетов кнопками, по нажатию — назначить
- [ ] Кнопка «◀️ Главное меню» возвращает reply-клавиатуру главного меню
- [ ] Написать тесты

### 1.4 Подменю «Отчёты»
- [ ] По нажатию «📊 Отчёты» — inline-кнопки: «Эта неделя», «Этот месяц», «Прошлый месяц», «За год», «◀️ Главное меню»
- [ ] По нажатию периода — вызвать существующую логику отчётов из intents.py
- [ ] Добавить кнопку «🤖 Анализ GPT» в каждый отчёт (уже есть для /report, распространить)
- [ ] Написать тесты

### 1.5 Подменю «Записать операцию»
- [ ] По нажатию «📝 Записать» — inline-кнопки: «💸 Расход», «💰 Доход», «🔄 Перевод», «◀️ Главное меню»
- [ ] Каждая кнопка запускает пошаговый диалог: сумма → категория (кнопками из списка) → счёт → подтверждение
- [ ] Текстовый и голосовой ввод продолжают работать параллельно
- [ ] Написать тесты

### 1.6 Подменю «Настройки»
- [ ] По нажатию «⚙️ Настройки» — inline-кнопки: «🕐 Таймзона», «📋 Google Sheets», «💎 Подписка», «◀️ Главное меню»
- [ ] «Таймзона» — показать текущую + кнопки популярных (Europe/Moscow, Europe/London, Asia/Almaty и т.д.)
- [ ] «Google Sheets» — показать статус + кнопки «Подключить», «Экспорт», «Импорт», «Отключить»
- [ ] «Подписка» — заглушка (реализация в Фазе 2)
- [ ] Написать тесты

### 1.7 Подменю «Помощь»
- [ ] По нажатию «❓ Помощь» — inline-кнопки: «📖 Инструкция», «ℹ️ О боте», «◀️ Главное меню»
- [ ] «Инструкция» — подробный текст с примерами (переработать текущий /help), разбить на страницы кнопками «Далее»/«Назад» если длинный
- [ ] «О боте» — версия, автор, описание, ссылки (GitHub, поддержка)
- [ ] Написать тесты

### 1.8 Глобальная кнопка меню
- [ ] Убедиться что ВО ВСЕХ ответах бота (отчёты, подтверждения, ошибки) есть кнопка «◀️ Главное меню» или возврат reply-клавиатуры
- [ ] Проверить все callback_handler пути — везде добавить кнопку возврата
- [ ] Написать интеграционные тесты на навигацию

## Фаза 2: Интеграция ЮKassa

### 2.1 Модель данных
- [ ] Добавить модель `Subscription` в `db/models.py`: id, user_id (FK), plan (enum: trial/monthly/yearly), payment_id (str, nullable для trial), amount (Decimal), paid_at (DateTime), expires_at (DateTime), status (enum: active/expired/cancelled)
- [ ] Добавить поля в модель User: `trial_used` (Boolean, default=False), `trial_activated_at` (DateTime, nullable)
- [ ] Убрать поля `free_insights_used` и `free_insights_reset_at` из User (не нужны — весь функционал только по подписке)
- [ ] Создать таблицу через init_db (Base.metadata.create_all)
- [ ] Написать тесты для модели

### 2.2 Сервис биллинга
- [ ] Создать `services/billing.py`
- [ ] Функция `activate_trial(user_id)` — создаёт Subscription с plan="trial", expires_at=now+14 дней, ставит user.trial_used=True. Отказывает если trial_used уже True
- [ ] Функция `create_payment(user_id, plan)` — создаёт платёж через ЮKassa SDK, возвращает URL оплаты
- [ ] Функция `check_payment_status(payment_id)` — проверяет статус платежа через ЮKassa API
- [ ] Функция `activate_subscription(user_id, plan, payment_id)` — записывает подписку в БД
- [ ] Функция `is_pro(user_id)` — проверяет есть ли активная подписка (trial, monthly или yearly) с expires_at > now
- [ ] Добавить `yookassa` в requirements.txt
- [ ] Добавить `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY` в env.example
- [ ] Написать тесты для billing.py (мокать ЮKassa SDK)

### 2.3 Paywall (экран без подписки)
- [ ] Создать функцию `send_paywall(update, user)` — красивый экран с описанием возможностей бота
- [ ] Текст paywall: краткое описание фич бота (учёт финансов, отчёты, аналитика, дайджесты, бюджеты)
- [ ] Кнопки: «🎁 Попробовать 14 дней бесплатно» (только если user.trial_used == False), «Месяц — 190₽», «Год — 1900₽»
- [ ] По нажатию trial — вызвать activate_trial, показать «✅ Пробная подписка активирована до ДД.ММ.ГГГГ — пользуйтесь!» + главное меню
- [ ] По нажатию покупки — создать платёж ЮKassa, отправить ссылку
- [ ] Написать тесты

### 2.4 Middleware проверки подписки
- [ ] Создать функцию `check_subscription(user_id)` — проверяет is_pro и возвращает True/False
- [ ] Встроить проверку в НАЧАЛО каждого хендлера (message_handler, menu_button_handler, все callback-обработчики)
- [ ] Исключения (работают БЕЗ подписки): /start, активация trial, покупка подписки, просмотр статуса подписки
- [ ] Если подписка не активна → показать paywall вместо обработки
- [ ] Написать тесты на middleware

### 2.5 UX подписки в настройках
- [ ] В «Настройки» → «💎 Подписка» — показать текущий статус (Trial/Pro до ДД.ММ.ГГГГ / Нет подписки)
- [ ] Если подписка активна — показать дату окончания + кнопку «Продлить»
- [ ] Если нет подписки — показать paywall
- [ ] За 3 дня до окончания — автоматическое напоминание о продлении
- [ ] При истечении — уведомить + показать paywall при следующем обращении
- [ ] Написать тесты

### 2.6 Онбординг с trial
- [ ] В онбординге (после создания первого счёта) — предложить активировать пробный период
- [ ] Кнопка «🎁 Активировать 14 дней Pro бесплатно» — сразу активирует trial
- [ ] Если пользователь пропускает — всё равно показать paywall при первой попытке использования

## Фаза 3: Retention-фишки

### 3.1 Модели данных
- [ ] Добавить модель `Budget` в models.py: id, user_id (FK), category (str), monthly_limit (Decimal), currency (str), created_at
- [ ] Добавить модель `UserStats` в models.py: id, user_id (FK), streak_days (int), last_activity_date (Date), total_operations (int), achievements_json (JSON)
- [ ] Написать тесты

### 3.2 Планировщик задач
- [ ] Создать `services/scheduler.py` — настройка JobQueue из python-telegram-bot
- [ ] Зарегистрировать джобы: еженедельный дайджест (воскресенье 20:00 по TZ юзера), ежемесячный (1-е число), проверка стриков (ежедневно), проверка подписок (ежедневно)
- [ ] Подключить scheduler в main.py
- [ ] Написать тесты

### 3.3 Еженедельный дайджест
- [ ] Создать `services/retention.py`
- [ ] Функция `generate_weekly_digest(user_id)` — собирает данные за неделю, сравнивает с прошлой
- [ ] Формирует текст через LLM (живой, мотивирующий): «На кофе на 500₽ меньше — так держать!»
- [ ] Отправляет пользователю через бот с кнопками «📊 Подробный отчёт», «◀️ Главное меню»
- [ ] Только для Pro-пользователей (или первые 2 недели бесплатно как trial)
- [ ] Написать тесты

### 3.4 Ежемесячный дайджест
- [ ] Функция `generate_monthly_digest(user_id)` — итоги месяца, сравнение, тренды
- [ ] Те же принципы что и еженедельный
- [ ] Написать тесты

### 3.5 Бюджеты по категориям (Pro)
- [ ] Добавить intent «set_budget» в LLM-промпт: «бюджет на кофе 3000» → создать бюджет
- [ ] В меню «Настройки» добавить «📊 Бюджеты» — список бюджетов + кнопки создания/удаления
- [ ] При записи расхода — проверить бюджет, уведомить при 80% и 100%
- [ ] В отчёте — показать прогресс по бюджетам
- [ ] Написать тесты

### 3.6 Стрики и ачивки
- [ ] При каждой записи операции — обновить streak_days и total_operations в UserStats
- [ ] Проверить и выдать ачивки: «Первая операция», «7 дней подряд 🔥», «30 дней подряд 🏆», «100 операций», «Сэкономил 10к за месяц»
- [ ] Показывать ачивку сразу после её получения
- [ ] В профиле — список полученных ачивок
- [ ] Написать тесты

### 3.7 Напоминания
- [ ] Если нет операций 3+ дня — отправить мягкое напоминание: «Давно не видел записей, всё в порядке? 😊»
- [ ] Не чаще 1 раза в 3 дня
- [ ] Пользователь может отключить в настройках
- [ ] Написать тесты

## Фаза 4: Полировка и финализация

### 4.1 Тесты
- [ ] Тесты на все новые intent-обработчики
- [ ] Тесты на навигацию по меню (из любого раздела можно вернуться в главное)
- [ ] Тесты на ограничения бесплатной версии
- [ ] Тесты на планировщик
- [ ] Интеграционные тесты end-to-end

### 4.2 Документация
- [ ] Обновить /help с описанием всех новых возможностей
- [ ] Раздел «О боте» — версия, описание, автор, контакты
- [ ] Обновить README.md — новая архитектура, скриншоты, фичи
- [ ] Обновить env.example с новыми переменными

### 4.3 Инфраструктура
- [ ] Обновить requirements.txt (yookassa, APScheduler если нужен)
- [ ] Обновить Dockerfile и docker-compose.yml
- [ ] Проверить все edge cases: нет счетов, нет операций, истекла подписка, нет интернета
- [ ] Финальный прогон всех тестов
