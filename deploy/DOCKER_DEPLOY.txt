════════════════════════════════════════════════════════════════
🐳 ДЕПЛОЙ ЧЕРЕЗ DOCKER (РЕКОМЕНДУЕТСЯ)
════════════════════════════════════════════════════════════════

ОДИН РАЗ: Загрузи google_credentials.json на сервер:

scp ~/Downloads/rich-uncle-scrooge-9a8c29733c56.json root@31.130.131.71:/var/www/smartfinances/google_credentials.json

════════════════════════════════════════════════════════════════

ДЕПЛОЙ (скопируй всю команду в веб-консоль Timeweb):

cd /var/www/smartfinances && docker-compose down 2>/dev/null; docker-compose pull 2>/dev/null || git pull origin main; docker-compose up -d --build; sleep 5; echo ""; echo "✅ БОТ ЗАПУЩЕН!"; docker-compose ps; echo ""; echo "📊 Логи:"; docker-compose logs --tail=20

════════════════════════════════════════════════════════════════

ПОЛЕЗНЫЕ КОМАНДЫ:

# Логи бота
docker-compose logs -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Статус
docker-compose ps

════════════════════════════════════════════════════════════════
✅ С Docker больше НЕТ проблем с дубликатами и правами!
════════════════════════════════════════════════════════════════

